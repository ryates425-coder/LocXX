<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- shortEdges: window can draw into cutout; WindowInsets still report cutout for Compose padding. -->
    <style name="Theme.LocXX" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Edge-to-edge: avoid scrim that can make inset handling inconsistent on OEM skins. -->
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
